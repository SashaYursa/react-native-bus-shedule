import { View, Text } from 'react-native'
import React from 'react'

type Props = {}

const BusShedule: React.FC<Props> = (props: Props) => {
  return (
    <View>
      <Text>BusShedule</Text>
    </View>
  )
}

export default BusShedule